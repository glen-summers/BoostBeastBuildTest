using msvc ;

# use pch, add multiple test apps

project
    : requirements
		<threading>multi 
		<address-model>64
		<architecture>x86
		<link>static
		<runtime-link>static
		<include>tmp/openssl/include
		<library-path>tmp/openssl/lib
    ;

using boost : 1.67 : <include>tmp/boost_1_67_0 <library>tmp/boost_1_67_0/stage/lib ;
import boost ;
boost.use-project 1.67 ;

lib libssl ;
lib libcrypto ;

exe App1
	: App1.cpp libssl libcrypto
	: <library>/boost//system
	;